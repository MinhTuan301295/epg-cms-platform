import { Typography } from 'antd';

export function DashboardPage() {
  return (
    <section>
      <Typography.Title level={2}>Dashboard</Typography.Title>
      <Typography.Paragraph>Operational summary for channels, assets, and schedules.</Typography.Paragraph>
    </section>
  );
}
